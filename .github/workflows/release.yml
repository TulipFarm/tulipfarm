name: Prepare release

# A maintainer starts this workflow through `pnpm release <version>`. It updates
# the version and changelog on a dedicated branch, opens a ready-for-review PR,
# and stops. Merging that PR is the approval that starts publish-image.yml.
on:
  workflow_dispatch:
    inputs:
      version:
        description: "Exact SemVer to release (for example, 0.5.0 or 0.5.0-beta.0)"
        required: true
        type: string

concurrency:
  group: prepare-release
  cancel-in-progress: false

permissions:
  actions: write
  contents: write
  pull-requests: write

jobs:
  prepare:
    name: Open release PR
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      GH_TOKEN: ${{ secrets.RELEASE_TOKEN || github.token }}
      RELEASE_TOKEN: ${{ secrets.RELEASE_TOKEN }}
      VERSION: ${{ inputs.version }}
    steps:
      - name: Require main
        run: |
          set -euo pipefail
          [ "$GITHUB_REF" = "refs/heads/main" ] || {
            echo "Release preparation must run from main, not $GITHUB_REF"
            exit 1
          }
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
          token: ${{ secrets.RELEASE_TOKEN || github.token }}
      - uses: ./.github/actions/setup
      - name: Validate release request
        run: |
          set -euo pipefail
          node --input-type=module -e '
            import { isReleaseVersion } from "./scripts/request-release.ts";
            if (!isReleaseVersion(process.env.VERSION ?? "")) {
              throw new Error("Invalid release version: " + process.env.VERSION);
            }
          '
          tag="v$VERSION"
          branch="release/$tag"
          if git rev-parse --verify --quiet "refs/tags/$tag"; then
            echo "Tag $tag already exists"
            exit 1
          fi
          if git ls-remote --exit-code --heads origin "$branch" >/dev/null 2>&1; then
            echo "Branch $branch already exists"
            exit 1
          fi
      - name: Generate version and changelog
        run: node scripts/release-changelog.ts generate "$VERSION"
      - name: Verify generated files
        run: |
          set -euo pipefail
          expected_version="$(node -p "require('./package.json').version")"
          [ "$expected_version" = "$VERSION" ] || {
            echo "Release tooling generated $expected_version instead of $VERSION"
            exit 1
          }
          changed="$(git diff --name-only)"
          [ -n "$changed" ] || {
            echo "Release tooling did not generate any changes"
            exit 1
          }
          while IFS= read -r file; do
            case "$file" in
              CHANGELOG.md|package.json) ;;
              *)
                echo "Unexpected generated file: $file"
                exit 1
                ;;
            esac
          done <<< "$changed"
          node scripts/release-changelog.ts validate "$VERSION"
      - name: Commit release proposal
        run: |
          set -euo pipefail
          branch="release/v$VERSION"
          git switch -c "$branch"
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add CHANGELOG.md package.json
          git commit -m "chore(release): v$VERSION"
          git push --set-upstream origin "$branch"
      - name: Open release PR
        env:
          BODY_FILE: ${{ runner.temp }}/release-pr.md
        run: |
          set -euo pipefail
          branch="release/v$VERSION"
          {
            echo "## Summary"
            echo
            echo "- prepare TulipFarm v$VERSION"
            echo "- update the package version and generated changelog"
            echo "- publish the verified GHCR image and GitHub Release automatically after merge"
            echo
            echo "## Test plan"
            echo
            echo "- [ ] Required PR checks pass"
            echo "- [ ] Version and changelog are approved"
          } > "$BODY_FILE"
          pr_url="$(gh pr create \
            --base main \
            --head "$branch" \
            --title "chore(release): v$VERSION" \
            --body-file "$BODY_FILE")"
          gh pr merge "$pr_url" --auto --squash
      - name: Start CI when using the workflow token
        if: ${{ env.RELEASE_TOKEN == '' }}
        run: |
          set -euo pipefail
          # GitHub suppresses push/PR workflows created by GITHUB_TOKEN. A
          # workflow_dispatch is allowed, so attach the normal checks to the
          # release branch explicitly when no RELEASE_TOKEN is configured.
          #
          # Both are required: publish-image.yml's `Verify merged source` asserts the
          # recorded conclusions of `CI Success`, `Docker build`, `Compose parity`, and
          # `Installer smoke` on the release commit. `CI Success` comes from ci.yml; the
          # other three come from container.yml. Dispatching only ci.yml would leave
          # three required checks unreported and block publication.
          gh workflow run ci.yml --ref "release/v$VERSION"
          gh workflow run container.yml --ref "release/v$VERSION"
      - name: Summarize
        run: |
          {
            echo "## Release v$VERSION proposed"
            echo
            echo "Auto-merge is enabled on the release PR. Publication starts automatically after merge."
          } >> "$GITHUB_STEP_SUMMARY"
