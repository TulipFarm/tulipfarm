# Tulip Surface Protocol

`@tulipfarm/surface` owns TSP contracts, the semantic component catalog, Artifact validation,
interaction contracts, linting, renderer interfaces, and declarative Soul component validation.

The package is runtime-neutral. It must not import React, browser APIs, or provider SDKs. Persisted
content is semantic data only: never HTML, CSS, JavaScript, executable templates, or provider
payloads.
