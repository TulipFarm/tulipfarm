---
title: Docker Compose
description: Run TulipFarm from the Compose file alone — no .env, no key generation. The file every self-host platform consumes.
---

import { Callout } from "fumadocs-ui/components/callout";

The [one-line installer](/docs/self-hosting/install) is the fastest path on a machine you have a
shell on. When you deploy through a UI instead — Portainer, Coolify, Dokploy, Unraid — you
paste a Compose file into a form, and there is no shell to run a script in. TulipFarm's
`docker-compose.yml` is written for exactly that: **every variable has a default, and no
`.env` is required.**

## Run it

Download `docker-compose.yml` and bring it up:

```bash
curl -fsSLO {{SITE_URL}}/docker-compose.yml
docker compose up -d
```

Open `http://localhost:8080` and finish the [setup wizard](/docs/self-hosting/install#finish-setup).

That is the whole procedure. Four services start: `app` (API, built web UI, and
migrations), `worker` (runs, waits, outbox, cron, and maintenance consumers),
`integration-worker` (Slack and GitHub ingress, sync, delivery, retries, and
reconciliation), and `postgres` (PostgreSQL 17 with pgvector).

<Callout type="info">
  Deploying through a UI instead? The same file goes straight into
  [Coolify](/docs/self-hosting/coolify), [Portainer](/docs/self-hosting/portainer), or
  [Dokploy, Unraid, Synology, and CasaOS](/docs/self-hosting/other-platforms) — those pages cover
  only what differs per platform.
</Callout>

## What happens to your secrets

TulipFarm needs three secrets: `ENCRYPTION_KEY` (which encrypts every secret the instance
stores — LLM keys, integration credentials), `JWT_SECRET`, and `WEBHOOK_SIGNING_SECRET`.

If you do not supply them, the app **generates them on first boot** and writes them to
`/data/secrets.env` inside the `tulipfarm-data` volume, `mode 0600`. Every later boot reads
them back from that file, so sessions and encrypted secrets survive restarts and upgrades.
The first-boot log prints the path in a banner you cannot miss.

<Callout type="warn">
  **Back up the `tulipfarm-data` volume.** It holds the key that decrypts every secret this
  instance stores. Losing it means losing them — restoring the database alone is not
  enough, because the database only holds ciphertext.

  If the app starts with a freshly generated `ENCRYPTION_KEY` against a database that
  already contains encrypted secrets, it **refuses to boot** rather than quietly orphaning
  them. That error means the data volume was lost: restore it, or set `ENCRYPTION_KEY`
  back to its original value.
</Callout>

Environment variables always win. Set `ENCRYPTION_KEY` yourself — from your platform's
secret store, or `openssl rand -base64 32` — and the file lane is never used. That is what
the installer does, and it is what you should do for anything beyond a personal instance:
a generated key sits on the same host as the database volume, so a full host compromise
yields both.

## Things worth changing

Put these in a `.env` file next to `docker-compose.yml`, or in your platform's environment
form. All are optional.

| Variable | Default | Why you would set it |
| --- | --- | --- |
| `HOST_PORT` | `8080` | Publish on a different host port. |
| `PUBLIC_URL` | `http://localhost:8080` | The address the app assumes until you save the real one in **Business → About**. Drives CORS and the cookie `Secure` flag. |
| `PUBLIC_API_URL` | `PUBLIC_URL` | Public origin used for integration OAuth callbacks and webhooks. Set it separately only when the API has a different public origin. |
| `PUBLIC_ORIGINS_LOCKED` | `false` | Make environment origins authoritative and read-only in Business → About. |
| `POSTGRES_PASSWORD` | `tulipfarm` | Replace the default database password. |
| `TULIPFARM_VERSION` | `latest` | Pin one image tag for `app`, `worker`, and `integration-worker` instead of tracking `latest`. |

<Callout type="warn">
  The bundled database defaults to the password `tulipfarm`. That is what lets the file run
  with no prior setup — Postgres demands a password at init time and cannot ask the app for
  one. It is safe **only** because port 5432 is never published to the host. Do not publish
  it. On anything reachable from a network you do not control, set `POSTGRES_PASSWORD`.
</Callout>

### The public address and the Secure cookie flag

The public address decides whether session cookies are marked `Secure`:

- `https://…` → cookies are `Secure`. Correct behind TLS.
- `http://…` → cookies are not. Browsers discard `Secure` cookies sent over plain HTTP
  (localhost excepted), so marking them on an HTTP origin would make login silently fail.

`PUBLIC_URL` supplies that address at boot. Once you save one under **Business → About**, the
saved value takes over and applies without recreating the container — so a wrong `PUBLIC_URL` is
something you correct in the app, not a redeploy.

Leave it at the default for a local instance; set it to `http://<your-lan-ip>:8080` for a
LAN box; set it to `https://your-domain` once TLS is in front. Set `PUBLIC_ORIGINS_LOCKED=true`
if you would rather the environment stay authoritative and the screen stay read-only.

## Use a managed database instead

Point `DATABASE_URL` at your own PostgreSQL 17, set `EXTERNAL_DATASTORE=true`, and start
the app, both workers and the bundled bucket — only `postgres` is then never created:

```bash
docker compose up -d app worker integration-worker bucket
```

Full walkthrough, including the extensions your provider must allow:
[Use a managed database](/docs/self-hosting/managed-database).

## Where uploaded and generated files go

The stack ships its own S3 server, the `bucket` service, so uploading an image for an
agent to read — or downloading a document one wrote — works with no cloud account.

Its access key is not in the compose file and is not a default. `app` generates it on
first boot, creates the bucket over the server's admin API, and writes the credentials to
`/data/bucket.env` at `mode 0600`, the same place and pattern as the other generated
secrets. The bucket publishes no host port: a file is authorized per request by `app`, so
a bucket a browser could reach would be a way around that check.

<Callout type="warn">
  **Back up the `tulipfarm-bucket` volume too.** It holds the file bytes. The
  `tulipfarm-data` volume holds the key to reach them; the database holds who may see
  what. All three are needed to restore an instance.
</Callout>

### Use an external S3 provider instead

Amazon S3, Cloudflare R2, Backblaze B2, Wasabi, DigitalOcean Spaces, Ceph and MinIO all
speak the same protocol, and TulipFarm has one driver for all of them. Turn the bundled
server off by setting `BUCKET_ADMIN_URL` to an empty value, then supply your own:

```bash
BUCKET_ADMIN_URL=
S3_BUCKET=my-bucket
S3_REGION=eu-central-1
S3_ENDPOINT=https://s3.eu-central-1.amazonaws.com
S3_ACCESS_KEY_ID=...
S3_SECRET_ACCESS_KEY=...
```

Start without the bundled server:

```bash
docker compose up -d app worker integration-worker postgres
```

Omit `S3_ENDPOINT` for Amazon S3 itself. Path-style addressing turns on automatically
whenever `S3_ENDPOINT` is set, which is what MinIO, Ceph and any host reached by IP need.

### Running without Compose

Nothing above is Compose-specific. A process started by hand with none of these variables
set stores files on disk under `TF_DATA_DIR`, which is the right answer for development
and the wrong one for anything that runs more than one replica or restarts its container.
Set `S3_BUCKET` and the two credentials to move it to a bucket; naming a bucket is enough
to select the S3 driver, and `BLOB_STORE` only exists to force the choice either way.

## Health checks

Two endpoints, for the two questions an orchestrator asks:

| Process | Port | Liveness | Readiness |
| --- | --- | --- | --- |
| `app` | `8080` | `/livez` | `/readyz`; `/health` is an alias. |
| `worker` | `4020` | `/livez` | `/readyz`; also checks the schema floor and required consumers. |
| `integration-worker` | `4030` | `/livez` | `/readyz`; also checks the schema floor and required consumers. |
| `bucket` | — | `garage status` | Same command; the container has no HTTP client to probe with. |

Only `app` publishes a host port. The worker probe ports are internal to Compose. Give
startup probes a generous timeout: database migrations run in `app` before it starts
listening, and both workers wait for healthy `app` because they never migrate.

`bucket` starts after `app` rather than before it, because `app` writes the two secrets
it boots from. On a first boot it may exit and restart once or twice while it waits for
them, which is normal and costs a few seconds.

## Update

```bash
docker compose pull && docker compose up -d
```

Migrations apply automatically on boot, and there are **no down-migrations** — read
[Updating and rollback](/docs/self-hosting/updating) before your first upgrade.

## Running more than one business on one machine

Copying `docker-compose.yml` into a second directory does **not** give you a second
instance. Compose tells stacks apart by **project name**, not by directory, and the file
pins one:

```yaml
name: tulipfarm
```

So a second `docker compose up -d` reuses the first stack's containers and volumes. Both
businesses end up sharing one database and one soul repo. Nothing errors — the second
simply comes up on the first's data, which is why this is worth knowing before you hit it.

Give each stack its own project name and host port:

```bash
HOST_PORT=8081 docker compose -p acme up -d
HOST_PORT=8082 docker compose -p globex up -d
```

That produces separate volumes per stack — `acme_tulipfarm-soul`, `globex_tulipfarm-soul`,
and so on. Then keep using the same `-p` for every later command; one without it targets
`tulipfarm`, not your stack:

```bash
docker compose -p acme logs -f app
docker compose -p acme down
```

Setting `COMPOSE_PROJECT_NAME` in each directory's `.env` is the less error-prone option —
it applies to every command in that directory without the flag. With the installer, use
[`TF_PROJECT_NAME`](/docs/self-hosting/install#running-a-second-instance-on-one-machine) instead.

<Callout type="warn">
  Do not rename an **existing** install's project. Volumes are named after it, so the
  renamed stack starts empty and the old database and soul repo are left orphaned under
  the previous name.
</Callout>

## Scale-out is not supported

Run **exactly one `app` replica**, **one maintenance `worker`**, and **one
`integration-worker`**. The `app` owns the soul git worktree and applies migrations. The
`worker` executes every turn and owns cron/maintenance consumers through
`WORKER_MAINTENANCE=true`. Scale vertically; horizontal scaling is a deliberate design
constraint, not a missing feature.
