query ReadIssue($id: String!) {
  issue(id: $id) {
    id
    identifier
    title
    description
    url
    updatedAt
    priority
    estimate
    assignee {
      id
      name
    }
    state {
      id
      name
    }
    team {
      id
      name
      key
    }
  }
}
