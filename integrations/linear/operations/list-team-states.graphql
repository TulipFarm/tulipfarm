query ListTeamStates($teamId: String!, $first: Int!, $after: String) {
  team(id: $teamId) {
    id
    states(first: $first, after: $after) {
      nodes {
        id
        name
        type
        position
      }
      pageInfo {
        hasNextPage
        endCursor
      }
    }
  }
}
